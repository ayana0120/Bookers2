<p id="notice"><%= notice %></p>
  <% if flash[:complete] %>
    <div class="flash">
      <%= flash[:complete] %>
    </div>
  <% end %>
<%= form_for(book) do |f| %>
  <% if book.errors.any? %>
  <div class="error">
      <h3><%= book.errors.count %> errors prohibited this book from being saved:</h3>
    <ul>
    <% book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
    </ul>
  </div>
  <% end %>
    <h2>New book</h2>
      <form>
        <div class="form-group ">
          <label>Title</label>
          <input type="text" name="title" class="form-control">
        </div>
        <div class="form-group">
          <label for="bodyTextarea">Opinion</label>
          <textarea class="form-control" id="bodyTextarea"></textarea>
        </div>
        <button type="submit" class="btn btn-primary btn-block">Create Book</button>
      </form>
<% end %>